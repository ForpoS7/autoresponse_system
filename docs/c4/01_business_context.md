# Уровень 1 — Бизнес-контекст (Business Context)

> Система как «чёрный ящик». Показывает внешних акторов, их роли и бизнес-процессы,
> которые система автоматизирует. Внутренняя архитектура скрыта.

```mermaid
flowchart LR
    subgraph ACTORS["Акторы"]
        direction TB
        USER["👤 Соискатель
        ─────────────
        Ищет работу на HH.ru.
        Тратит часы на ручной
        поиск и написание
        сопроводительных писем"]

        EMPLOYER["🏢 Работодатель
        ─────────────
        Публикует вакансии
        на платформе HH.ru.
        Не взаимодействует
        с системой напрямую"]
    end

    SYS["⚙️  HH AutoResponse System
        ══════════════════════════════
        ▸ Авторизация и управление HH-сессией
        ▸ Поиск вакансий по критериям пользователя
        ▸ Персональная генерация сопроводительных писем
        ▸ Пакетная автоматическая подача откликов
        ▸ Отслеживание статусов и хранение истории
        ▸ Архивация данных в холодное хранилище"]

    subgraph EXT["Внешние системы"]
        direction TB
        HH["🌐 HH.ru
        ─────────────
        Платформа поиска работы.
        Источник вакансий
        и цель для откликов.
        Нет публичного API —
        работа через браузер"]

        LLM["🤖 Ollama · qwen3.5:7b
        ─────────────
        Локальный LLM-сервер.
        Генерирует персональные
        сопроводительные письма
        под каждую вакансию.
        Без внешних API-ключей"]
    end

    USER -->|"① Регистрация и вход в систему"| SYS
    USER -->|"② Настройка критериев поиска\n   (ключевые слова, зарплата, город)"| SYS
    USER -->|"③ Запуск пакетного автоотклика"| SYS

    SYS -->|"④ Статус откликов\n   (pending → in_progress → completed)"| USER
    SYS -->|"⑤ История и аналитика\n   по откликам"| USER

    EMPLOYER -->|"Размещает вакансии"| HH

    SYS -->|"Парсит список вакансий\n(Playwright · Firefox)"| HH
    SYS -->|"Отправляет отклик от\nимени соискателя с письмом\n(Playwright · Firefox)"| HH

    SYS -->|"POST /api/generate\n{title, company, requirements}"| LLM
    LLM -->|"Текст сопроводительного\nписьма (до 30 сек)"| SYS
```

## Бизнес-процессы, которые автоматизирует система

| № | Было (ручной процесс) | Стало (автоматизировано) |
|---|---|---|
| 1 | Ежедневный ручной поиск вакансий (1–2 ч/день) | Автоматический парсинг по расписанию |
| 2 | Написание уникального письма для каждой вакансии | Генерация через LLM за ~20 сек |
| 3 | Ручная подача каждого отклика на сайте | Playwright-автоматизация в фоне |
| 4 | Отсутствие истории откликов | Хранение в PostgreSQL + архив в MinIO |

## Границы ответственности

| Внутри системы | Вне системы |
|---|---|
| Управление JWT-сессией пользователя | Алгоритмы ранжирования вакансий HH.ru |
| Хранение HH-токена (Playwright storageState) | Решение работодателя о приглашении |
| Генерация писем через Ollama | Качество LLM-модели qwen3.5:7b |
| Архивация данных старше 30 дней | Политика HH.ru в отношении автоматизации |
